<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout cho từng item phòng trọ trong RecyclerView.
    Hiển thị: Mã phòng, Tên phòng, Giá thuê, Tình trạng (màu xanh/đỏ).
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="6dp"
    android:layout_marginEnd="6dp"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="14dp"
    app:cardElevation="4dp"
    app:rippleColor="?attr/colorControlHighlight">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Hàng 1: Mã phòng (trái) + Badge Tình trạng (phải) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvMaPhong"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="12sp"
                android:textColor="#9E9E9E"
                android:letterSpacing="0.05" />

            <!--
                Badge Tình trạng:
                - Màu CHỮ và NỀN được set lập trình trong PhongAdapter
                - Xanh lá = Còn trống  |  Đỏ = Đã thuê
            -->
            <TextView
                android:id="@+id/tvTinhTrang"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:paddingTop="5dp"
                android:paddingBottom="5dp"
                android:textStyle="bold"
                android:textSize="12sp" />

        </LinearLayout>

        <!-- Hàng 2: Tên phòng (in đậm, cỡ lớn) -->
        <TextView
            android:id="@+id/tvTenPhong"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="19sp"
            android:textColor="#1A237E"
            android:layout_marginTop="6dp" />

        <!-- Hàng 3: Giá thuê (màu xanh dương) -->
        <TextView
            android:id="@+id/tvGiaThue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="#1565C0"
            android:layout_marginTop="4dp" />

        <!-- Đường kẻ phân cách -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#EEEEEE"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="8dp" />

        <!-- Gợi ý thao tác -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="👆 Nhấn để sửa   ·   📌 Giữ để xóa"
            android:textSize="11sp"
            android:textColor="#BDBDBD"
            android:gravity="center" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
